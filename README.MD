# Sandbox React Native App - 2019

## HOW TO START
- npm install --verbose
- expo start
- change *initialRouteName* to *Loading* in *AppNavigator* first time you start the project to load the data from the API, return it to *Home* again after fetching data.